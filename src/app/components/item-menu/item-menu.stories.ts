import { moduleMetadata } from '@storybook/angular';
import { ItemMenuComponent } from './item-menu.component';

export default {
  title: 'general/item/item-menu',
  component: ItemMenuComponent,
  //decorators: [metadata],
};

export const Default = () => ({});
